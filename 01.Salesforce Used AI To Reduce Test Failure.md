### Article Link
  * <a href="https://engineering.salesforce.com/how-ai-test-automation-cut-developer-productivity-bottlenecks-by-30-at-scale/">How AI Test Automation Cut Developer Productivity Bottlenecks by 30% at Scale</a>
  * <a href="https://blog.bytebytego.com/p/how-salesforce-used-ai-to-reduce">How Salesforce Used AI To Reduce Test Failure Resolution Time By 30%</a>

### The Problem
  * Salesforce runs a huge automated testing pipeline: ~6 million tests per day, covering ~78 billion possible test combinations. 
  * They deal with ~150,000 test failures per month, with ~27,000 code changelists (code changes) submitted daily. 
  * Before AI automation, develoeprs spent hours manually - navigating logs, change sets, and bug-tracking systems (GUS. 
 
### Goals for the AI System
  * Reduce the manual effort of diagnosing failures. 
  * Provide context-aware, clear recommendations to developers that help them fix issues quickly. 
  * Build trust in AI tools by giving reliable (avoiding vague) and precise (incorrect) suggestions.

### AI and Automation Architecture
  * Salesforce built an AI-powered “TF (Test Failure) Triage Agent” that, when a failure occurs, quickly suggests potential fixes. 
  * This architecture had to work with massive amounts of noisy, unstructured error data while keeping response times (very low latency) under 30 seconds 
  <img width="700" height="500" alt="image" src="https://github.com/user-attachments/assets/5bedb4da-f70d-476a-a626-7710846395d7" />

### Technical Architecture
  1. Semantic Search with FAISS (Facebook AI Similarity Search)
    * Salesforce used FAISS to create a semantic search index of historical test (past) failures and their resolutions.
    * FAISS is a library that allows very fast similarity searches between data represented as vectors
    * When a new failure shows up, they perfoms a vector similarity search for similar historical failures. 
    <img width="700" height="400" alt="image" src="https://github.com/user-attachments/assets/d04000ac-a81e-4598-8933-53050c7a79ca" />
  2. Contextual Embeddings & Parsing: (messy) Error logs and stack traces are cleaned and contextual embedded (representations). so AT can compare new errors to past ones meaningfully. 
  3. Asynchronous & Decoupled Pipelines: The triage system runs separately (not blocking their CI/CD), so it can run in parallel and return suggestions fast. 
  4. Hybrid Approach - LLM Reasoning and Semantic Search: After finding similar past failures (via semantic search), an LLM refines and reason over them to generate clear, context-specific guidance. 

### How They Built It
  * They used Cursor, an AI-assisted pair-programming tool, to help build the TF Triage Agent.
  * With Cursor, what could have taken “months to build” was delivered in 4–6 weeks.
  * Cursor helped by auto-generating boilerplate / retrieval code, letting engineers focus on the core triage logic.
  <img width="700" height="400" alt="image" src="https://github.com/user-attachments/assets/687f0b5f-80b5-4062-9539-159fa1a8db9f" />

### Results & Impact
  * After deployment, test-failure (TF) resolution time dropped by ~30%, from ~7 days to 2–3 days on average
  * It significantly boosted developer productivity, reduced friction, and lowered the backlog of failures. 
  * They rolled it out gradually and carefully to build trust, using real data to show that the suggestions were useful. 

### Key Learnings / Lessons
  * Vector search + contextual embeddings make it possible to match new problems to previously solved ones very effectively. 
  * Combining semantic search with LLM reasoning yields more precise and trustworthy recommendations. 
  * Asynchronous design is critical: by decoupling the triage from the CI/CD flow, latency stays low and scalability is maintained. 
  * Using AI dev tools (like Cursor) can dramatically speed up building internal AI systems. 
  * Incremental rollout + data-backed trust-building is important when introducing AI workflows to engineers. 

### Why It Matters / Insights
  * This is a real-world example of how large-scale engineering orgs can practically apply AI to significantly improve developer workflows.
  * Instead of using AI for high-level tasks only, Salesforce is using it for developer productivity, reducing mundane, manual work.
  * The hybrid approach (semantic search + LLM) seems more effective than just LLM or just search: it grounds suggestions in real, historical data.
  * The engineering tradeoff: they invest in building such a system, but the payback is huge in time saved, reduced backlog, and faster resolution.

## Code Example
 * Below is a practical, end-to-end sample implementation of the core ideas used in the Salesforce Test Failure (TF) Triage Agent:
    - Vector embeddings for logs
    - FAISS similarity search
    - Hybrid workflow (retrieval → LLM reasoning)
    - Async pipeline pattern
    - Architecture like Salesforce but simplified
* This is real, runnable reference code (Python).
* It won’t match Salesforce’s scale, but it shows you exactly how to build a similar system.

### 1. Sample Architecture (Code-Oriented)
 * New Test Failure → Embedding → FAISS Search → Retrieve Past Cases → LLM → Recommendation

### 2. Sample Data Structure
 * Historical failure DB (could be SQL, BigQuery, S3, MySQL)
 ```
 [
   {
     "id": "TF-001",
     "error_log": "NullPointerException in PaymentService on line 42",
     "resolution": "Initialize payment context before calling validate()."
   },
   {
     "id": "TF-002",
     "error_log": "Timeout while connecting to Redis cache",
     "resolution": "Increase Redis connection pool size."
   }
 ]
 ```

### 3. Code: Embeddings + FAISS Index Build
```
import json
import faiss
import numpy as np
from sentence_transformers import SentenceTransformer

model = SentenceTransformer("all-MiniLM-L6-v2")

# Load historical failures
with open("historical_failures.json", "r") as f:
    failures = json.load(f)

logs = [f["error_log"] for f in failures]
embeddings = model.encode(logs, convert_to_numpy=True)

# Build FAISS index
dim = embeddings.shape[1]
index = faiss.IndexFlatL2(dim)
index.add(embeddings)

faiss.write_index(index, "tf_index.faiss")
print("FAISS index created with", len(logs), "failure entries")
```

### 4. Code: Querying a New Failure
```
import faiss
import numpy as np

# Load created index
index = faiss.read_index("tf_index.faiss")

def find_similar_failures(error_log, top_k=5):
    vec = model.encode([error_log], convert_to_numpy=True)
    # Limit top_k to number of items in index to avoid FAISS padding with -1
    effective_k = min(top_k, index.ntotal)
    distances, indices = index.search(vec, effective_k)
   
    results = []
    for dist, idx in zip(distances[0], indices[0]):
        # FAISS uses -1 for missing entries; skip them
        if idx < 0 or dist > 1e37:
            continue
        results.append({
            "match_score": float(dist),
            "failure": failures[idx]
        })
    return results

new_log = """
NullPointerException when processing invoice batch.
Stack trace: PaymentService.java:41
"""

matches = find_similar_failures(new_log)
print(matches)
```
 * <img width="738" height="385" alt="image" src="https://github.com/user-attachments/assets/0a556614-6c57-4a53-b0ff-6f46d8c182fb" />
 * FAISS will always return k results (k=5), EVEN IF:
    - old code ```distances, indices = index.search(vec, top_k)```
    - the index has fewer than 5 vectors
    - it must pad missing entries with invalid placeholders
 * FAISS pads with:
   - index = -1
   - distance = max_float (≈ 3.4e38) => ```340282346638528859811704183484516925440.0000```
  * It's FAISS telling you:
    - ❌ “There were no more real neighbors — this result is fake.”
  * Example: You have 2 vectors, but requested 5, FAISS returns:
    
   |Result	|Real?	|Index	|Distance
   |-|-|-|-
   |1	|✔	0	|real |score |
   |2	|✔	1	|real |score
   |3	|❌ |padded	|-1	|3.4e38
   |4	|❌ |padded	|-1	|3.4e38
   |5	|❌ |padded	|-1	|3.4e38
 
 * FIX
   ```
    effective_k = min(top_k, index.ntotal)
    distances, indices = index.search(vec, effective_k)

     if idx < 0 or dist > 1e37:
   ```
  
### 5. Code: LLM Reasoning Layer (Hybrid Approach)
 * This takes retrieved similar failures and refines them into a final recommendation.
 * Using OpenAI API (works with any LLM):
 ```
 from openai import OpenAI
 client = OpenAI()
 
 def reason_over_matches(new_log, matches):
     prompt = f"""
 You are an AI triage agent. A new test failure occurred. Analyze the new error,
 compare it to historical failures, and propose the most likely root cause + fix.
 
 New Error Log:
 {new_log}
 
 Top Similar Historical Failures:
 {matches}
 
 Based on patterns, output:
 1. Root cause hypothesis
 2. Recommended fix
 3. Confidence level (0–100)
 """
     response = client.chat.completions.create(
         model="gpt-4o-mini",
         messages=[{"role": "user", "content": prompt}]
     )
 
     return response.choices[0].message.content
 ```

### 6. Full End-to-End Example
 ```
 new_log = """
 NullPointerException at PaymentService.validate() 
 when processing order batch.
 """
 
 matches = find_similar_failures(new_log)
 final_result = reason_over_matches(new_log, matches)
 
 print("\n=== Final Recommendation ===")
 print(final_result)
 ``` 
 * Output example:
 ```
 Root Cause:
 PaymentService.validate() is being called before the payment
 context object is initialized.
 
 Recommended Fix:
 Initialize context in PaymentContextFactory before validate().
 Add null check in PaymentService.
 
 Confidence: 86%
 ```

### 7. Async/Decoupled Pipeline Example (FastAPI + Queue)
 * This mimics Salesforce's non-blocking CI/CD integration.
 * Triage API (FastAPI)
```
from fastapi import FastAPI
import asyncio
from queue import Queue

app = FastAPI()
triage_queue = Queue()

@app.post("/submit_failure")
async def submit_failure(payload: dict):
    triage_queue.put(payload["error_log"])
    return {"status": "queued"}

async def triage_worker():
    while True:
        while not triage_queue.empty():
            log = triage_queue.get()
            matches = find_similar_failures(log)
            result = reason_over_matches(log, matches)
            print("Triage result:\n", result)
        await asyncio.sleep(0.1)

@app.on_event("startup")
async def startup():
    asyncio.create_task(triage_worker())
```

 * CI/CD can call:
 ```
 POST /submit_failure  
 { "error_log": "...stack trace..." }
 ```

 * This mirrors Salesforce:
   - Decoupled
   - Fast response
   - Doesn’t block pipeline

### 8. Optional: Storing Results in DB
```
import sqlite3

def store_resolution(new_log, recommendation):
    conn = sqlite3.connect("triage.db")
    c = conn.cursor()
    c.execute("""
        INSERT INTO triage_results (log, recommendation)
        VALUES (?, ?)
    """, (new_log, recommendation))
    conn.commit()
    conn.close()
```

### 9. Putting It All Together (Pipeline Function)
```
def run_triage(new_log):
    matches = find_similar_failures(new_log)
    result = reason_over_matches(new_log, matches)
    store_resolution(new_log, result)
    return result
```

### 10.Complete Code
 * requirements.txt, Install dependencies: "pip install -r requirements.txt"
 ```
 faiss-cpu
 sentence-transformers
 numpy
 openai
 ```
 * all.py
 ```
 # 1. Embeddings + FAISS Index Build
 import json
 import faiss
 import numpy as np
 from sentence_transformers import SentenceTransformer
 
 model = SentenceTransformer("all-MiniLM-L6-v2")
 
 # Load historical failures
 with open("historical_failures.json", "r") as f:
     failures = json.load(f)
 
 logs = [f["error_log"] for f in failures]
 embeddings = model.encode(logs, convert_to_numpy=True)
 
 # Build FAISS index
 dim = embeddings.shape[1]
 index = faiss.IndexFlatL2(dim)
 index.add(embeddings)
 
 faiss.write_index(index, "tf_index.faiss")
 print("FAISS index created with", len(logs), "failure entries")
 
 #2. Querying a New Failure
 # Load created index
 index = faiss.read_index("tf_index.faiss")
 
 def find_similar_failures(error_log, top_k=5):
     vec = model.encode([error_log], convert_to_numpy=True)
     distances, indices = index.search(vec, top_k)
 
     results = []
     for i, idx in enumerate(indices[0]):
         results.append({
             "match_score": float(distances[0][i]),
             "failure": failures[idx]
         })
     return results
 
 #3. LLM Reasoning Layer (Hybrid Approach)
 import os
 from openai import OpenAI
 
 azure_openai_client = True
 openai_client = False
 github_client = False
 
 client = None
 deployment_name = None
 
 if azure_openai_client:
     endpoint = "https://es.openai.azure.com/openai/v1"
     deployment_name = "EsGPT-5"
     api_key = "B2s1q...3dQQ"
 
     client = OpenAI(
         base_url=endpoint,
         api_key=api_key
     )
    
     print("Using Azure OpenAI")
 elif openai_client:
     openai_key = os.getenv("OPENAI_API_KEY")  
     client = OpenAI(api_key=openai_key)                     
     deployment_name = "gpt-4o-mini"
     print("Using OpenAI")
 elif github_client:
     github_token = os.getenv("GITHUB_TOKEN") 
     client = OpenAI(base_url="https://models.inference.ai.azure.com", api_key=github_token)
     deployment_name = "gpt-4o-mini"
     print("Using GitHub")
 else:
     print("Warning: No AI provider credentials set (Azure/OpenAI/GitHub). Reasoning will be skipped.")
    
 def reason_over_matches(new_log, matches):
     prompt = f"""
 You are an AI triage agent. A new test failure occurred. Analyze the new error,
 compare it to historical failures, and propose the most likely root cause + fix.
 
 New Error Log:
 {new_log}
 
 Top Similar Historical Failures:
 {matches}
 
 Based on patterns, output:
 1. Root cause hypothesis
 2. Recommended fix
 3. Confidence level (0–100)
 """
     response = client.chat.completions.create(
         model=deployment_name, #"gpt-4o-mini",
         messages=[{"role": "user", "content": prompt}]
     )
 
     return response.choices[0].message.content
 
 
 new_log = """
 NullPointerException at PaymentService.validate() 
 when processing order batch.
 """
 
 matches = find_similar_failures(new_log)
 
 print("\n=== Similar Failures Found ===")
 for i, match in enumerate(matches, 1):
     print(f"\n{i}. Match Score: {match['match_score']:.4f}")
     print(f"   Historical Failure: {match['failure']}")
 
 final_result = reason_over_matches(new_log, matches)
 
 print("\n=== Final Recommendation ===")
 print(final_result) 
 ```
<img width="1435" height="808" alt="image" src="https://github.com/user-attachments/assets/149e5f6b-9b6e-41a6-8845-90d3ff5ac2f0" />
